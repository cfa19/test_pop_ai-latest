# Core API Framework
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# AI/ML Providers
openai>=1.0.0
voyageai>=0.2.0

# Database & Auth
supabase>=2.0.0
pyjwt>=2.8.0

# Workflow Orchestration
langgraph>=0.0.25

# Core ML Libraries (Required)
transformers>=4.30.0
torch>=2.0.0
datasets>=2.12.0
sentence-transformers>=2.2.0

# Training Utilities (Required)
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0,<2.0.0

# Performance & Optimization (Recommended)
accelerate>=0.20.0        # Faster training with automatic mixed precision
evaluate>=0.4.0           # Evaluation metrics

# Language Detection & Translation
langdetect>=1.0.9
lingua-language-detector>=2.0.0
# fasttext: optional; requires C++ build (often fails on Windows). Install manually if needed:
#   pip install fasttext   (Linux/WSL) and set LANG_DETECT_FASTTEXT_MODEL_PATH to lid.176.bin
googletrans
