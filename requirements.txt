# Core API Framework
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.25.0

# AI/ML Providers
openai>=1.0.0
voyageai>=0.2.0

# Database & Auth
supabase>=2.0.0
pyjwt>=2.8.0

# Workflow Orchestration
langgraph>=0.0.25

# ML Inference (ONNX Runtime — no PyTorch needed)
onnxruntime>=1.15.0
transformers>=4.30.0
numpy>=1.24.0,<2.0.0

# Language Detection & Translation
langdetect>=1.0.9
googletrans

# Optional providers (install if CHAT_MODEL uses them)
# anthropic>=0.25.0       # for claude-* models
# google-generativeai     # for gemini-* models
